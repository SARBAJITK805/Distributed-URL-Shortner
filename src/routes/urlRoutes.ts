import express from "express";
const router= express.Router();

router.post("/shorten",()=>{
    try {
        
    } catch (error) {
        
    }
})

router.get("/:shortCode",()=>{
    try {
        
    } catch (error) {
        
    }
})

router.get("/info/:shortCode",()=>{
    try {
        
    } catch (error) {
        
    }
})

export default router;
